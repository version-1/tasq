# Codex App-Server Contract

Tasq launches Codex app-server as a local subprocess through `bash -lc <codex.command>` with the
per-issue workspace as the process working directory. The repository workflow sets
`codex.command` to `codex --sandbox workspace-write app-server` so the app-server starts with a
workspace-write sandbox posture inside the development container.

The installed Codex CLI schema is the protocol source of truth. The runner's message shape is based
on the schema generated by:

```sh
codex app-server generate-json-schema --out <dir>
```

The runner separates Codex orchestration from transport framing. The orchestration layer owns
JSON-RPC request IDs, response correlation, thread and turn sequencing, approval denial policy, and
runner event emission. Transport implementations only provide connection lifecycle and byte-frame
send/receive operations.

The current production wiring still starts the stdio subprocess transport. Runtime transport
selection is intentionally out of scope for the first websocket implementation.

The stdio transport uses the v2 app-server JSON-RPC line protocol:

- `initialize` with `clientInfo` and `experimentalApi`.
- `initialized` notification after initialize succeeds.
- `thread/start` with the absolute workspace path as `cwd` and a persistent thread
  (`ephemeral: false`) so later runs can resume it by thread ID.
- `turn/start` with the same `cwd`, the returned `threadId`, and one text input containing the
  rendered workflow prompt.
- `turn/completed` notification as success.
- `error`, subprocess exit, context cancellation, response timeout, or turn timeout as failure.

The websocket transport follows the OpenAI Codex app-server contract documented at
<https://developers.openai.com/codex/app-server/>:

- Start app-server with a `--listen ws://IP:PORT` endpoint and connect to that URL.
- The transport is experimental and unsupported upstream.
- Local listeners such as `ws://127.0.0.1:PORT` are the supported Tasq target for this phase.
- Tasq does not add authentication headers, subprotocols, or Origin headers in this phase.
- Each WebSocket text frame contains exactly one JSON-RPC message.
- Connection close, failed frame reads, and context deadlines are surfaced as transport errors to the
  JSON-RPC session layer.
- The same listener exposes `/readyz` and `/healthz`, but Tasq's client does not depend on health
  probes for the initial implementation.

Tasq currently treats server-to-client requests such as approval or dynamic tool requests as
unsupported JSON-RPC errors. This prevents unattended runs from waiting indefinitely for operator
input.

Command-execution and file-change approval requests are known approval requests. Tasq immediately
responds with `{"decision":"cancel"}` and fails the run with an `approval_required` error that
includes the approval method and raw request payload. The dispatcher then marks a still-ready issue
or still-in-progress issue as `blocked` and writes those details to the blocker comment so an
operator can decide whether a future retry should allow the requested action. Approval, sandbox, and
user-input behavior should be
revisited before enabling wider production usage.

Runner progress is persisted to SQLite in `runner_events`. The current implementation does not write
separate large transcript artifacts to the filesystem.
